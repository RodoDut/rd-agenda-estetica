# Copy this file to .env and fill the values. DO NOT commit .env (it's ignored).

# SFTP / FTP server settings
SFTP_HOST=145.223.77.254
SFTP_PORT=21
SFTP_PROTOCOL=ftp
SFTP_USER=u938009968.rdtecnobelleza.net
SFTP_PASSWORD=REPLACE_ME
SFTP_REMOTE_PATH=/public_html
SFTP_CONTEXT=c:/rdtecnobelleza
SFTP_PASSIVE=true

# If you want to use an SSH key instead of password, set:
# SFTP_PROTOCOL=sftp
# SFTP_PRIVATE_KEY=~/.ssh/id_ed25519
# SFTP_PASSPHRASE=

# Notes:
# - Keep the real .env file local and private; do not commit it.
# - Use `generate-sftp.ps1` (Windows) or `generate-sftp.sh` (Unix) to create .vscode/sftp.json from .env.
